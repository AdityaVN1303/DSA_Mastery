next_permutations(str/arr) 
- (To get all permutations , sort the str/arr first)
- It modifies the current string by reference.
- It returns bool. 

sort(it1 , it2)
sort(it1 , it2 , greater<int>)
- Sorts the Actual DS
- Works on Strings , Vectors and Deque.

accumulate(it1 , it2 , initialSum);
- Returns sum of all element from it1 to it2 starting from intitial value as inititalSum. 

__builtin_popcount(num)
- int count = __builtin_popcount(13);
- Returns count of 1's in the binary representation of the number. 

max_element(itr1 , itr2)/min_element(itr1 , itr2)
- Value is accessed using *()
- Works with Vectors , Strings and Deque. 

max(a , b) / min(a , b)
- Returns Largest/Smallest among both

swap(a , b)
- Swaps both number. 

pow(base , expo)
- Used to calculate exponentiation

INT_MIN / INT_MAX 
- Gives Min/Max Value for that Integer

LLONG_MIN / LLONG_MAX
- Gives Min/Max Value for that Long

DBL_MAX / -DBL_MAX
- Gives Min/Max Value for that Double 
